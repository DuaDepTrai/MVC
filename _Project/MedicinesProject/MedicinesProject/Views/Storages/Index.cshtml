@model IEnumerable<MedicinesProject.Storages>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="sticky-header mb-3 p-3 bg-light border rounded">
    <div class="d-flex justify-content-between align-items-center flex-wrap">
        <p class="mb-2">
            @Html.ActionLink("Add New Receipt", "Create", null, new { @class = "btn btn-primary" })
        </p>

        @using (Html.BeginForm("Index", "Storages", FormMethod.Get, new { @class = "form-inline d-flex flex-wrap justify-content-end" }))
        {
            <div class="d-flex" style="width: 600px;">
                <div class="d-block" style="width: 550px; margin-right: 5px;">
                    <div style="width: 550px;">
                        <input type="text" name="searchStr" id="searchStr" class="form-control mr-2 mb-2" style="width: 550px;"
                           placeholder="Search by Receipt/Batch/Name/Active Ingredient/Supplier" value="@ViewBag.CurrentSearch" />
                    </div>
                    <div style="vertical-align: central; width:550px">
                        @Html.DropDownList("activeIngredientFilter",
                                new SelectList(ViewBag.ActiveIngredients as List<string>, ViewBag.ActiveIngredientFilter as string),
                                "Filter Active Ingredients",
                                new { @id = "activeIngredientFilter", @class = "form-control select2 mr-2 mb-0" })
                        @Html.DropDownList("supplierFilter",
                                new SelectList(ViewBag.Suppliers as List<string>, ViewBag.SupplierFilter as string),
                                "Filter Suppliers",
                                new { @id = "supplierFilter", @class = "form-control select2 mr-2 mb-0" })

                    </div>
                </div>
                <div class="d-block">
                    <button type="submit" class="btn btn-primary mb-1">
                        <i class="fas fa-search"></i>
                    </button>
                    <a href="@Url.Action("Index", "Medicines")" class="btn btn-primary mb-0 ml-0">
                        <i class="fas fa-sync-alt"></i>
                    </a>
                </div>
            </div>
        }
    </div>
</div>

<table class="table table-striped">
    <thead>
        <tr>
            <th style="width: 8%;">
                Receipt Code
            </th>
            <th style="width: 8%;">
                Batch Number
            </th>
            <th style="width: 9%;">
                Trade Name
            </th>
            <th style="width: 9%;">
                Active Ingredient
            </th>
            <th style="width: 10%;">
                Manufacture Date
            </th>
            <th style="width: 7%;">
                Expiry Date
            </th>
            <th style="width: 5%;">
                Quantity
            </th>
            <th style="width: 7%;">
                Import Price
            </th>
            <th style="width: 7%;">
                Import Date
            </th>
            <th style="width: 22%;">
                Supplier
            </th>
            <th style="width: 8%;">
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ReceiptCode)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.BatchNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Medicines.TradeName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Medicines.ActiveIngredient)
                </td>
                <td style=" text-align: center;">
                    @item.ManufactureDate.ToString("dd-MM-yyyy")
                </td>
                <td style=" text-align: center;">
                    @item.ExpiryDate.ToString("dd-MM-yyyy")
                </td>
                <td style=" text-align: right;">
                    @Html.DisplayFor(modelItem => item.Quantity)
                </td>
                <td style=" text-align: right;">
                    @item.ImportPrice.ToString("N0")
                </td>
                <td style=" text-align: center;">
                    @item.ImportDate.ToString("dd-MM-yyyy")
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Suppliers.SupplierName)
                </td>
                <td class="action-column" style=" text-align: center;">
                    <a href="@Url.Action("Details", new { id = item.StorageID })" class="btn btn-info btn-sm">
                        <i class="fas fa-info-circle"></i>
                    </a>
                    <a href="@Url.Action("Edit", new { id = item.StorageID })" class="btn btn-warning btn-sm">
                        <i class="fas fa-edit"></i>
                    </a>
                    <a href="@Url.Action("Delete", new { id = item.StorageID })" class="btn btn-danger btn-sm">
                        <i class="fas fa-trash-alt"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>

@if (ViewBag.TotalPages > 1)
{
    int maxPagesToShow = 10;
    int currentPage = ViewBag.CurrentPage;
    int totalPages = ViewBag.TotalPages;

    int startPage = Math.Max(1, currentPage - (maxPagesToShow / 2));
    int endPage = Math.Min(totalPages, startPage + maxPagesToShow - 1);

    if (endPage - startPage < maxPagesToShow - 1)
    {
        startPage = Math.Max(1, endPage - maxPagesToShow + 1);
    }

    <div class="pagination-container text-center" id="pagination-inventory">
        <div class="pagination d-inline-block">
            <ul class="pagination" style="display: flex; list-style: none; padding: 0; margin: 0; font-size: 12px;">
                <li style="margin: 0 5px;">
                    <a href="@(currentPage == 1 ? "#" : Url.Action("Index", new { page = currentPage - 1 }))"
                       style="padding: 8px 15px; border: 1px solid #ddd; border-radius: 4px; color: #007bff; text-decoration: none; transition: background-color 0.3s, color 0.3s; font-weight: bold; @(currentPage == 1 ? "opacity: 0.5; cursor: not-allowed;" : "")"
                       @(currentPage == 1 ? "onclick=\"return false;\"" : "")>
                        « Prev
                    </a>
                </li>

                @for (int i = startPage; i <= endPage; i++)
                {
                    <li style="margin: 0 5px;" class="@(i == currentPage ? "active" : "")">
                        <a href="@Url.Action("Index", new { page = i })"
                           style="padding: 8px 15px; border: 1px solid #ddd; border-radius: 4px; color: @(i == currentPage ? "white" : "#007bff"); text-decoration: none; transition: background-color 0.3s, color 0.3s; @(i == currentPage ? "background-color: #007bff;" : "")">
                            @i
                        </a>
                    </li>
                }

                <li style="margin: 0 5px;">
                    <a href="@(currentPage == totalPages ? "#" : Url.Action("Index", new { page = currentPage + 1 }))"
                       style="padding: 8px 15px; border: 1px solid #ddd; border-radius: 4px; color: #007bff; text-decoration: none; transition: background-color 0.3s, color 0.3s; font-weight: bold; @(currentPage == totalPages ? "opacity: 0.5; cursor: not-allowed;" : "")"
                       @(currentPage == totalPages ? "onclick=\"return false;\"" : "")>
                        Next »
                    </a>
                </li>
            </ul>
            <p style="padding-top: 10px">Page @currentPage of @totalPages</p>
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/jqueryval")
    <!-- Thêm Select2 CSS và JS -->
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>
    <script>
        $(document).ready(function () {
            // Khởi tạo Select2 cho dropdown ActiveIngredientFilter
            $('#activeIngredientFilter').select2({
                placeholder: "Filter Active Ingredients",
                allowClear: true,
                width: '49.5%',
                dropdownAutoWidth: true
            });

            // Đảm bảo giá trị được chọn vẫn gửi qua form
            $('#activeIngredientFilter').on('select2:select select2:clear', function (e) {
                $(this).closest('form').submit();
            });

            // Khởi tạo Select2 cho dropdown SupplierFilter
            $('#supplierFilter').select2({
                placeholder: "Filter Suppliers",
                allowClear: true,
                width: '49.5%',
                dropdownAutoWidth: true
            });

            // Đảm bảo giá trị được chọn vẫn gửi qua form
            $('#categoryFilter').on('select2:select select2:clear', function (e) {
                $(this).closest('form').submit();
            });
            
        });
    </script>
}