@model IEnumerable<HRM8.SEX>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SexName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SexName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SexID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SexID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SexID })
        </td>
    </tr>
}

</table>
